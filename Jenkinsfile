pipeline {
         agent any
         stages {
                 stage('Build') {
                 steps {
                     echo 'Hi, this is Anoop'
					 echo 'We are now checking out code from GIT'
					 input('Do you want to proceed?')
                 }
                 }
                 stage('Code Coverage') {
                 steps {
                    
                 }
                 }
         }
         }
